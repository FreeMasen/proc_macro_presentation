# Before Our First Example
$slides-only$
- Separate crate
- `Cargo.toml` declaration
  - ```toml
    [lib]
    proc-macro = true  
    ```
- `extern crate proc_macro;`
$slides-only-end$
$web-only$

$web-only-end$